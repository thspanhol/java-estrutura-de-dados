package equalsHashcode;

public class Map {/*

MAP

A interface Map em Java é parte da Java Collections Framework e fornece uma estrutura para armazenar e manipular
pares chave-valor. Diferentemente das interfaces List ou Set, que contêm apenas elementos, a interface Map é
usada para associar chaves a valores. Cada chave mapeia para um único valor, e uma chave pode ser associada a no
máximo um valor.

Implementações Comuns:

HashMap: Uma implementação baseada em tabela hash.
         Não garante ordem das chaves, e permite uma chave null e múltiplos valores null.

TreeMap: Implementa Map usando uma árvore vermelha-preta. Garante que as chaves estarão ordenadas de forma natural
         ou através de um Comparator fornecido.

LinkedHashMap: Mantém uma lista duplamente ligada dos elementos, preservando a ordem de inserção.
               Útil quando é necessário manter a ordem de inserção dos elementos.

ConcurrentHashMap: Uma implementação thread-safe de HashMap, permitindo que múltiplos threads modifiquem o mapa
                   simultaneamente sem a necessidade de sincronização explícita por parte do programador.
*/}
